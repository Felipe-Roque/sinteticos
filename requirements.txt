# Core dependencies for the sinteticos toolkit and CLI
pandas>=1.3
numpy>=1.21
matplotlib>=3.4
seaborn>=0.11
openpyxl>=3.0
# SDV provides CTGAN, GaussianCopula, CopulaGAN models
# Note: depending on platform, SDV may pull optional extras (e.g., torch). See SDV docs if build issues occur.
sdv>=1.8
# Fallback for GaussianCopula when SDV is unavailable
copulas>=0.8

# For CFA in Python
semopy>=2.3